<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="TinyButtons" parent="android:Widget.Button.Small">
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:width">35dp</item>
        <item name="android:height">35dp</item>
        <item name="android:textSize">17dp</item>
     </style>


    <style name="BiggerButtons" parent="android:Widget.Button.Small">
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:width">80dp</item>
        <item name="android:height">80dp</item>
        <item name="android:textSize">40dp</item>
    </style>

    <style name="gameViewMobile">
        <item name="android:layout_width">105dp</item>
        <item name="android:layout_height">105dp</item>
    </style>

    <style name="gameViewTablet">
        <item name="android:layout_width">240dp</item>
        <item name="android:layout_height">240dp</item>
    </style>

</resources>
